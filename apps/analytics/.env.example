# Analytics Go Service
DATABASE_URL=postgresql://...
PORT=8080

# API base URL (live-update + form-notify). In Docker use http://api:4000
API_URL=http://localhost:4000

# Shared with API for POST /api/analytics/live-update and /api/analytics/form-notify
WEBHOOK_SECRET=
